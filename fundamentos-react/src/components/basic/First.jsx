import React from "react";

export default () => {
    const title = "Componente React";
    const subTitle = "Este é o meu primeiro componente react!";

    return (
        <div>
            <h1>{title}</h1>
            <p>{subTitle}</p>
        </div>
    );
}